# DIY.md — Manual tasks for Swayam (human-only)
Format/rules: see `AGENTS.md` § Logging protocols. Check "Pending" before each session; move to "Done" (with date) once **you** have confirmed it, not when an agent assumes it.

Cross-lane requests (Days 2–4) also land here: when a lane needs a change inside another lane's file, it logs the ask here instead of reaching across. The integrator resolves them.

---

## Pending — Day 0 (do all of these before Day 1 starts; Day 1 stalls without them)

- [x ] **(Day 0)** Create the GitHub repo, add all four teammates as collaborators with push access, note the remote URL.
- [x] **(Day 0)** Protect `main`: require a PR to merge, no direct pushes. This is what makes the lane model hold.
- [x] **(Day 0)** Create the Supabase project. Record project URL, anon key, and service-role key. **Service-role key is backend-only — never in any `frontend/` env var or client bundle.**
- [x] **(Day 0)** LLM API key for the offline localization script (Claude or GPT — pick one, note cost/rate-limit reasoning in `DECISIONS.md`). Confirm the key works with one throwaway call before Day 1.
- [x] **(Day 0)** Confirm GPU access for fine-tuning: Kaggle (T4, ~30h/week free) or Colab. Log in, start a throwaway session, confirm you actually get a GPU allocated — do this on Day 0, not at hour 6 of Day 1.
- [x] **(Day 0)** Confirm OSHA SIR data reaches your machine (direct download or Kaggle mirror + login). If the agent's sandboxed network can't fetch it, you download it manually and drop it in `data/raw/`.
- [x] **(Day 0)** Local toolchain present: Node 20+, Python 3.11+, git, `uv` or `venv`. Verify each with a version command.
- [x] **(Day 0)** Vercel account (frontend) + Render account (backend). Create them now; connect billing if required. Deploying for the first time at 2am on Day 1 is where this plan dies.

## Pending — Days 1–4

- [~] **(Day 1)** Decide the real `sif_potential` labeling rule with your own eyes on the OSHA severity columns, before the agent generates a single label. This is a judgment call, not an agent task — it is the one thing a judge is most likely to interrogate.
- [~] **(Day 1)** Manually read ~20 localized narratives before the full 2,000-row generation runs. You are checking that they sound like Indian oil-rig reports and that the labels aren't obviously wrong. Log the review in `AUDIT.md`.
- [x] **(Day 1, BLOCKING all DB verification)** Fix `backend/.env`. It currently holds the FRONTEND variables (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_API_BASE_URL`) - a copy of `frontend/.env.local`. `backend/database.py` reads `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`, which exist in no `.env` in this repo, so it raises `KeyError` on import. The anon key cannot substitute: `select` on `sites` returns `42501 permission denied` (no grants, RLS deferred to Block 7). Copy `backend/.env.example` and fill both values from the Supabase dashboard (Settings -> API). Until this is done every endpoint in Block 5 is unverified against a real database - see `AUDIT.md` 2026-08-25 for exactly which paths the stubbed verification did not cover.
- [x] **(Day 1)** Final human review before the first public deploy — the agent flags "ready," it does not push to production URLs unprompted.
- [x] **(Day 1, BLOCKING Block 3)** Approve or reject the model substitution. You asked for `llama-3.1-8b-instant`; it returns 404 and the account has NO Llama chat model. I set `openai/gpt-oss-20b` (same gpt-oss family the prompts were tuned against, separate daily quota). It is NOT prompt-validated: the 20/20 prose pass was measured on the 120b, and the one row re-run on the 20b downgraded a hospital admission to an on-site infirmary visit and invented two closing sentences. Either confirm the 20b, or wait for the 120b's daily reset.
- [x] **(Day 1, BLOCKING Block 3)** Decide how to pay for the generation run. Measured 1,520 tokens/row x 1,200 rows = ~1.9M tokens against a ~200,000/day/model free-tier ceiling: roughly 9-10 days per model bucket, not the ~4 hours the per-minute rate suggests. Options: upgrade to Groq Dev Tier, run it across several days on the `.jsonl` checkpoint, or cut the row count. Lane A gets whatever `split_dataset.py` produces from the rows that exist.
- [ ] **(Day 1)** Nobody has started the generation run. The command is `python scripts/localize_dataset.py --count 1200 --workers 1`; stop it any time with Ctrl-C and every completed row survives in `data/processed/localized.jsonl`.
- [ ] **(Day 7)** Get an OIL operations SME to resolve the 11 acronyms in `backend/preprocessing/oil_acronyms.py` UNVERIFIED (`dsv`, `ht`, `lt`, `pm`, `cp`, `mop`, `tt`, `sh`, `wd`, `temp`, `oim`). Each has two plausible readings; they pass through unexpanded until someone who knows confirms them.
- [ ] **(Day 2)** Send each teammate their lane brief (from `meta_roadmap.md` Day 2) **before** they open an agent session.
- [ ] **(Day 4)** Confirm the actual demo-day network (venue wifi vs. phone hotspot) and test latency on it, not on home broadband.
- [ ] **(Ongoing)** You are the sole integrator. Every merge to `main` and every change to a FROZEN file goes through you.

- [x] **(Day 1, Block 7)** Sign off retroactively on the two-line change to FROZEN `backend/main.py` (one import, one `include_router`) that registers the new `routes/sites.py`. The Intake site selector has no data source without it; `DECISIONS.md` 2026-08-26 has the reasoning. Reject it and the selector must be re-planned, not patched.
- [x] **(Day 1, BLOCKING both-roles verification)** Create the two demo accounts in Supabase Auth **with `app_metadata.role` set** — one `hse_manager`, one `site_supervisor`. It must be `app_metadata`, not `user_metadata`: the latter is writable by the user themselves, so a role there can be self-promoted from the browser console (`DECISIONS.md` 2026-08-26). Set it with the service-role key or the dashboard, e.g. `supabase.auth.admin.updateUserById(id, { app_metadata: { role: 'hse_manager' } })`. Until this exists every session reads "no role set" and lands on `/intake`, so the Block 7 exit line "verify both roles actually land correctly" cannot be closed (`AUDIT.md` 2026-08-26).
- [x] **(Day 1, BLOCKING Confirm/Override) — DONE IN BLOCK 7B, NEEDS YOUR CONFIRMATION ONLY.** I inserted one `users` row per Supabase Auth account, with `users.id` set equal to the auth uid: `d8542ffe-…` (supervisor@oil.com, `site_supervisor`) and `03e55fd8-…` (manager@oil.com, `hse_manager`). Every id was read from `supabase.auth.admin.list_users()`, none invented; `site_id` is left null. The pre-existing hand-made row `00000000-…-0001` was left untouched — **delete it if you do not want it**, since it matches no auth account and can never be a real reviewer. The 422 is gone and both review paths are verified (`AUDIT.md` 2026-08-26). Confirm the two rows look right to you, then move this to Done. Original ask below, for the record: `schema.sql` creates it and seeds nothing, and `classifications.reviewed_by` is a foreign key to it — so every review returns `422 reviewed_by is not a known user`. Each row's `id` must equal the corresponding Supabase Auth uid, since that is what the review page sends. The UI shows that 422 verbatim rather than faking success, so the failure is visible, but the write path stays unverified until this is done.
- [x] **(Day 1, Block 7)** Decide whether `frontend/lib/user_role.ts` and `frontend/lib/precursor_spans.ts` join the FROZEN list. Both are cross-lane: `user_role.ts` is the privilege rule read by middleware, the header and login; `precursor_spans.ts` is the span maths behind the renderer both Intake and Detail use. I have not added them myself — the FROZEN list is yours.
- [x] **(Day 1)** Add a pinned `ruff` to `backend/requirements.txt`, or correct `CLAUDE.md`, which documents `ruff check .` as the backend lint command. It is installed in neither the venv nor `requirements.txt` (`AUDIT.md` 2026-08-26).

- [x] **(Day 1, Block 7B)** Decide whether `/review` should be role-gated. I added a "Review queue" link to the header visible to every signed-in role, because the queue is a workflow rather than a privileged view — the supervisor who filed a low-confidence report is often the person who can say what actually happened. `PRD.md` § Frontend pages gives item 5 no role restriction either way. If you want it `hse_manager`-only, that is a change to the privilege rule in `frontend/lib/user_role.ts` plus a `middleware.ts` check, not a link removal — the page would still be reachable by URL.
- [x] **(Day 1, Block 7B)** Decide what to do about the 3 pre-existing high-severity npm advisories (`postcss`, `sharp`, and `next` transitively). None came from `recharts`; all three predate this block. `npm audit fix --force` would move `next` off the pinned `15.5.23` that every verified page was built against, so I did not run it — it is a toolchain call for the integrator (`AUDIT.md` 2026-08-26, status open).
- [x] **(Day 1, Block 7B)** `STAGES.md` "Current Position" was stale on `backend/.env` — it listed the missing service-role key as blocking, but the file is fixed and the database is reachable. I have updated the block; flagging it because the same staleness may sit in notes you keep elsewhere.

- [ ] **(Day 1, Block 9)** Sign off retroactively on the change to FROZEN `backend/schemas.py`: a `strip_nul_bytes` field validator on `ReportCreate.raw_text` plus one `NUL_BYTE` constant. It fixes a real raw `HTTP 500` on report text containing U+0000 (`AUDIT.md` 2026-08-26). **No field name, type or nullability changed**, so the wire contract and `frontend/lib/api_client.ts` are untouched — but the file is FROZEN and contract-bearing, so it is your call. Reject it and the alternative is catching `22P05` in three separate insert paths instead of one guard; reasoning in `DECISIONS.md` 2026-08-26.
- [ ] **(Day 1, Block 9)** Sign off retroactively on the change to FROZEN `backend/main.py`: a `RequestValidationError` handler (plus a small `_scrub_unencodable`) that returns FastAPI's ordinary 422 body with unencodable characters removed. It fixes a raw `HTTP 500` where a lone surrogate crashed FastAPI's *own* 422 handler — validation worked, reporting it crashed. The 422 status and `detail` list shape are unchanged, verified against what `api_client.ts` parses. `main.py` is documented as app wiring only; an exception handler is app-level config rather than endpoint logic, which is why it went there and not in a router (`DECISIONS.md` 2026-08-26).
- [ ] **(Day 2, Lane A — after the Block 8 weight swap)** Re-run `backend/.venv/Scripts/python.exe scripts/seed_demo_reports.py`. All 26 rows in `classifications` today carry `model_version = 'interim-keyword-0.1'` and are STALE the moment real weights land — they are keyword output, not model output. Find them with `select count(*) from classifications where model_version = 'interim-keyword-0.1';`. Delete the interim-scored reports first, or the dashboard mixes two models' verdicts in one ranking.
- [ ] **(Day 1, Block 9 — closes an open AUDIT finding)** Do one real signed-in browser pass over both roles and confirm the empty states and a stored injection payload render as inert text. This is the one thing this block could NOT verify: there is no Playwright/Puppeteer in the repo and the pages are client-rendered, so `curl` returns the shell, not the populated state. The empty-database case was proven at the API layer (6/6) and the injection case structurally (no `dangerouslySetInnerHTML`/`innerHTML`/`eval` in any of 23 frontend files), but neither was seen on a screen (`AUDIT.md` 2026-08-26, status open). Same item as the still-open Block 7 exit line.
- [ ] **(Day 1, Block 9 item 4 — at deploy)** Re-measure ingest latency on the deployed URLs and bind the backend to a host the frontend reaches without a fallback. Locally, uvicorn binds IPv4-only on `127.0.0.1:8001` while `localhost` resolves to `::1` first, so every new connection paid a failed IPv6 attempt: `GET /health` 2649 ms via `localhost` vs 615 ms via `127.0.0.1`, and ~384 ms with a reused keep-alive connection on either. Real pipeline cost is ~384 ms (inference is 5.6 ms of it), well inside the 3s target — the 3s figure was Windows name resolution, not the pipeline (`AUDIT.md` 2026-08-26). Pair this with the existing Day 4 demo-network check.
- [ ] **(Day 2 / Lane A, Block 8 — SUPERSEDES the "do NOT let transformers reach backend/requirements.txt" item above)** That instruction could not be honoured and the reason is structural, not a shortcut: `backend/inference/` imports `torch` and `spacy` at module load, so without them in `backend/requirements.txt` the deployed backend cannot import its own inference module and **every** endpoint dies at startup, not just inference. `torch==2.11.0` (CPU), `transformers==5.0.0` and `spacy==3.8.16` are now pinned there; `scikit-learn` was kept out (offline-only, 163 MB). Measured: **885 MB site-packages + 537 MB weights against Render free tier's 512 MB ceiling** (`AUDIT.md` 2026-08-27). **Decide one:** pay for a larger Render instance, export quantized/ONNX weights (needs a Lane A training task, no verified pipeline today), or accept that the demo runs on localhost only. Nothing was pushed or deployed.
- [ ] **(Day 2 / Lane A, Block 8 — blocking, and independent of the memory ceiling above)** `model_weights/` is in `.gitignore` (line 31), so **the weights cannot reach Render by push at all.** A deploy today would start cleanly and then fail on the first inference call trying to read a directory that does not exist — a worse failure than not deploying, because it looks healthy until someone submits a report. **Decide one:** Git LFS, download-on-boot from object storage, bake them into an image, or leave the deployed backend on the pre-swap commit until the ceiling question is settled.
- [ ] **(Day 2 → Lane C, CROSS-LANE REQUEST — do not let Lane A edit this)** `routes/reports.py:95-97` feeds `cleaned_text` into all three inference functions, but all three models were trained on `raw_text` (`AUDIT.md` 2026-08-27, train/serve skew). Every model therefore sees a different text distribution at serve time than it was fitted on. Two possible fixes and they belong to different lanes: retrain on `cleaned_text` (Lane A) or change what the route passes (Lane C's file — Lane A must not touch it). Needs an integrator decision on which, before either lane acts.
- [ ] **(Day 2 / Lane A, Block 8)** 10 `classifications` rows still carry `model_version = 'interim-keyword-0.1'`. These are the earlier ad-hoc test rows, **not** the demo seed — no dataset file can regenerate them, and one of them holds the project's only human `overridden` review decision, so they were deliberately left alone rather than wiped for tidiness. The 20 demo-seed rows were replaced and all carry `distilbert-sif-1.0`. **Decide:** purge the 10 (loses that one real review decision), leave them (a `model_version` query returns a mixed answer), or re-submit their text through the real pipeline as new rows.
- [ ] **(Day 2, integrator — three stale doc references the weight swap invalidated; Lane A is not permitted to edit any of them)** (1) FROZEN `PATTERNS.md:467` still says the interim bodies carry "`INTERIM_LANE_A`, 6 occurrences, 2 per module" and § 9 item 1 still describes all three inference bodies as keyword matchers — both are now false, and § 8's "re-measure `sif_classifier.py` after the swap" is answered: 90 lines, under the limit, no split needed. (2) `README.md:81` still describes inference as the `INTERIM_LANE_A` keyword implementation. (3) `STAGES.md` "Current Position" still lists Block 8 under BLOCKED and says all 26 classifications are interim — Block 8 is done and the counts have changed (30 reports / 20 real + 10 interim classifications / 25 iogp_tags / 94 precursors). The existing "STAGES.md is stale" item above already covers part of this; this adds Block 8's specifics.

## Day 1, Block 9 item 4 (deploy) — DONE. Live on Vercel + Render + Supabase, smoke-tested end to end 2026-08-26.

- [x] **(Day 1, Block 9)** Deploy the backend. Done as a **Render.com Web Service** on the native Python 3 runtime, Root Directory `backend`, start command `uvicorn main:app --host 0.0.0.0 --port $PORT`, GitHub-integrated so a push to `main` deploys. This replaced the earlier Hugging Face Spaces plan — Spaces' Docker environments are paid (`DECISIONS.md` 2026-08-26). Consequence of the switch: no second git remote, so GitHub repo visibility is the only disclosure decision, and the root `Dockerfile`/`.dockerignore` are on no deploy path.
- [x] **(Day 1, Block 9)** Set the three Render environment variables before the first deploy: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `FRONTEND_ORIGINS`. `backend/database.py` reads the first two at *import* time, so a missing one is a `KeyError` before uvicorn binds a port — a failed deploy with the reason in Render's log, not a 500 from a live app.
- [x] **(Day 1, Block 9)** Set the three Vercel env vars with Root Directory = `frontend`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (**anon key, never the service-role key — it ships to the browser**), `NEXT_PUBLIC_API_BASE_URL` (the Render URL, **no trailing slash**).
- [x] **(Day 1, Block 9)** Put the actual Vercel production domain in Render's `FRONTEND_ORIGINS`. Verified by the live smoke test: zero CORS errors, which is what proves the origin matches scheme + host exactly (`AUDIT.md` 2026-08-26).
- [x] **(Day 1, Block 9)** Scrub `GEMINI_API_KEY` from `backend/.env`. Nothing under `backend/` reads it; it is only used by `scripts/localize_dataset.py`, which runs offline. Runtime and offline generation now share no credential (`AUDIT.md` 2026-08-26).
- [x] **(Day 1, Block 9)** Accept that **Vercel preview deployments will fail CORS**. Each preview gets its own hashed origin and the allowlist holds one origin, deliberately — the API has no auth and holds the service-role key. Demo from the production URL, or add the one specific preview origin you intend to use.
- [x] **(Day 1, Block 9)** Container-build risk retired, not resolved: `docker build` was never run here (no Docker daemon on this machine), and Render uses the native Python runtime, so no container is built at all.

- [ ] **(Day 1, Block 9)** Delete the root `Dockerfile` and `.dockerignore`, or add a one-line comment saying they are unused. They were written for HF Spaces and are now dead configuration that a teammate could mistake for the live deploy path.
- [ ] **(Day 1, Block 9)** Wake the Render service before the demo, not at it. Free-tier services spin down after inactivity and the first request pays a cold start. Fold this into the deployed-latency item above and the Day 4 demo-network check.
- [ ] **(Day 2/Block 8, before the weight swap)** Check Render free tier's **512 MB memory ceiling** against `torch` + `transformers` + DistilBERT. Today the image needs no ML library at all, so this is not yet a problem; it becomes the binding constraint the moment real weights land.

- [ ] **(Day 2 / Lane A, integrator decision) SWAP THE RETRAINED SIF WEIGHTS INTO `backend/model_weights/sif_classifier/`.** Lane A retrained after finding the epoch-1 checkpoint bug (`AUDIT.md` 2026-08-27) and left the converged run at repo-root `model_weights/sif_classifier/` **without touching the live weights**. Live today: test accuracy 0.5102, T 0.5 pinned to the grid edge, 49 of 49 test rows below 0.65. Retrained: **test accuracy 0.5918, T 1.201, 12 of 49 below 0.65**. The swap is `backend/model_weights/sif_classifier/` <- the four files in `model_weights/sif_classifier/` (`model.safetensors`, `config.json`, `calibration.json`, tokenizer files). Not done by Lane A because it changes what the live/demo system answers, and two follow-ons land with it, below.
- [ ] **(Day 2 / Lane A, blocks the swap above) FROZEN-FILE EDIT NEEDED: `backend/inference/sif_classifier.py` docstring becomes false the moment the weights are swapped.** Its `=== READ THIS BEFORE TRUSTING A VERDICT ===` block hardcodes the old numbers in prose — "validation accuracy 0.524 on n=42, confusion [[1,20],[0,21]]... it answers SIF on 41 of 42", "EVERY report routes to the Manual Review Queue and the auto-publish path effectively does not exist", and it names corpus size as the cause. All four claims are wrong for the retrained weights, and the cause attribution was wrong for the old ones too. The file is FROZEN, so Lane A did not edit it: integrator sign-off plus a `DECISIONS.md` entry. Leaving a stale block there is worse than a stale metric — it instructs the reader not to trust a model that now works better than it says.
- [ ] **(Day 2 / Lane A, after the swap) Re-run `scripts/tune_confidence_threshold.py` and re-seed the demo.** The 0.65 sweep in `AUDIT.md` 2026-08-27 concluded "no candidate above 0.55 publishes anything at all" against confidences spanning 0.501–0.567. The retrained model spans **0.519–0.874** on test, so that sweep's central finding no longer holds and 0.65 is a real cut point rather than a formality. The 20 seeded rows also all carry `needs_review` and confidences 0.509–0.551 from the old weights, so the Review Queue and the KPI cards will keep demonstrating the broken model until `seed_demo_reports.py` is re-run (it skips rows whose `raw_text` already exists — the 20 must be deleted first, as in the 2026-08-27 re-seed).
- [ ] **(Day 2 / Lane A) Re-run the two trainings on a Kaggle T4 if a GPU run is wanted for the record.** Lane A ran `train_sif_classifier.py` **to completion on local CPU** (90 steps, ~4 min) rather than on Kaggle, because the fix needed verifying, not just writing. The numbers in `AUDIT.md` are from that real local run. `train_iogp_tagger.py` and `build_ner_ruler.py` were **not** re-run and are unaffected by this fix — the tagger has its own separate weakness already logged.
- [ ] **(Day 2 / Lane A) `ruff` is not installed in `backend/.venv` and is in neither requirements file, so no lint ran this session.** `CLAUDE.md` asks for lint before exit criteria are met and `PATTERNS.md` already records the missing `ruff`; stating it rather than implying a clean lint. `py_compile` passes on all four changed files and the preprocessing self-check is 45/45. Add `ruff` to `scripts/requirements.txt` (dev-only — it must not reach `backend/requirements.txt`, which is already over Render's ceiling).
- [ ] **(Day 2 / Lane A) `clean_report.py` is 220 lines and `oil_acronyms.py` 213, both now over the ~200-line Boring Architecture limit.** `oil_acronyms.py` is pure data (one dict per group) and `clean_report.py` is three stage functions plus the contract, so neither has a responsibility seam to split on that would not create a worse file. Flagged for the integrator's judgment rather than split unilaterally or left unmentioned; the existing acceptances for 8 over-limit files are in `AUDIT.md` 2026-08-25.
- [ ] **(Day 2 / Lane A) The train/serve skew logged on 2026-08-27 is now slightly different, not resolved.** All three models are still trained on `raw_text` and served `cleaned_text`, and this session changed what `cleaned_text` contains (287 -> 110 corrupted words, contractions preserved). The serve-time text is now closer to clean English than the corpus the model was fitted on in some respects and further in others, so the skew's *direction* moved and its size is still unquantified. The real fix remains either retraining on `cleaned_text` or changing what `routes/reports.py` (Lane C's file) feeds in.

## Done

*(move confirmed items here with a date — don't delete)*

- [ ] **(Day 2, Lane B)** Add `activity` filter parameter to `GET /api/v1/reports` endpoint in `backend/routes/reports.py` to support drill-down from the "By Activity" density ranking table. Currently `site_id`, `sif_potential`, `iogp_rule`, `review_status` are supported; `activity` would need to match reports by activity precursor spans. This is a read-only query extension, not a data model change.
- [ ] **(Day 2, Lane B)** Add site UUID to DensityRow in FROZEN `backend/schemas.py`. Currently `DensityRow` contains only `group_name` (string), making it impossible for the frontend to drill down into a specific site's reports without a second lookup query. Need to add `group_id: UUID | None` field to `DensityRow` in schemas.py (Line 218). This is a FROZEN file requiring integrator sign-off and `DECISIONS.md` entry. Also requires updating `/api/v1/analytics/density` to return the UUID for each site.
- [ ] **(Day 2)** Integrator: `STAGES.md` "Current Position" is stale — BASELINE still says `data/processed/` holds 0 rows and the demo seed is 20; it now holds **326** (`localized.jsonl`), split 277 train / 49 test. Lane A did not edit the block, since four lanes share it and the integrator owns merges.
- [ ] **(Day 2)** Decide on `2025099811`: its localized text states "No permit was checked for this packing job" but its OSHA source never mentions a permit (`AUDIT.md` 2026-08-26). Drop the row, regenerate it, or accept it — Lane A did not edit reviewed data.
- [ ] **(Day 2 / Lane A, Block 6)** Run `scripts/train_sif_classifier.py` and `scripts/train_iogp_tagger.py` in a Kaggle T4 notebook (GPU on, internet on for the DistilBERT download), then download `model_weights/sif_classifier/` and `model_weights/iogp_tagger/` whole — the tokenizer and `calibration.json` / `tagger_metrics.json` matter as much as the weights. Edit only the CONFIG block at the top of each file; on Kaggle the paths need a `/kaggle/input/<dataset-name>/` prefix. Both were smoke-tested on CPU here and run top to bottom, but **no real metric exists until this run happens** (`AUDIT.md` 2026-08-26).
- [ ] **(Day 2 / Lane A, Block 6)** Paste each script's final "PASTE INTO AUDIT.md" block into `AUDIT.md` verbatim, including the bad numbers. Both blocks are generated from computed values, so they need no editing — and if a rule reads "not computable", leave it that way rather than substituting 0.
- [ ] **(Day 2 / Lane A, Block 8)** Apply the classifier's `calibration.json` temperature at inference when the weights are swapped in. Skipping it leaves raw over-confident softmax (~0.99 on wrong rows), which makes `CONFIDENCE_THRESHOLD = 0.65` route nothing to the Manual Review Queue and quietly empties the review screen. If the printed temperature carries the "pinned to the edge of the search grid" warning, treat the confidences as unusable and say so rather than shipping them.
- [ ] **(Day 2)** Integrator: do NOT let `transformers` / `scikit-learn` / `spacy` / `torch` reach `backend/requirements.txt`. They are in `scripts/requirements.txt` and the deployed backend must stay clear of them — Render's free tier has a 512 MB ceiling (already flagged above), and the Block 8 swap loads saved weights rather than training. Note `torch` is deliberately left unpinned there: pinning the locally verified `2.11.0+cpu` would downgrade a Kaggle GPU notebook to a CPU build and leave the T4 idle.
- [ ] **(Day 2)** Decide whether Confined Space (4 train / 0 test) and Hot Work (8 / 0) get more rows before the demo. Work Authorisation has **0 train examples** and is untrainable, not merely unmeasurable — the tagger's 9th output unit can only ever predict 0, so the model must not be described as covering all 9 IOGP rules (`DECISIONS.md` 2026-08-26). This is a talking point to prepare, not a bug to hide.
