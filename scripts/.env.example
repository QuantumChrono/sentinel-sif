# Groq API key for scripts/localize_dataset.py. Offline, one-time dataset generation only —
# nothing under backend/ imports this script, and no request-time path reads this key.
GROQ_API_KEY=
GEMINI_API_KEY=
